# Print value passed in by event object

Simple Lambda function that takes in values from event object and prints. The
value sent in will be converted to int prior to using within function

    {
    "msg": {
        "days": "70"
        }
    }
